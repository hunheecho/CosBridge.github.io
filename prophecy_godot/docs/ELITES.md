# 특수 정예 7종 (2026-09-08, 시험값)

사람 플레이 평가("일반 전투가 전반적으로 너무 쉽다 / 5일차에는 가만히 있어도 적이 접근하다 죽었다 /
한 대 맞았을 때 피해량은 나쁘지 않은데 몬스터 공격이 실제로 적중하는 일이 적다")에 대한 대응 중,
**체력만 높인 적이 아니라 고유 연계를 가진 정예**를 만든 결과다.

- 정의(행동 수치): `data/enemies.json` → `enemies.elite_*`
- 규칙(행동): `scripts/rules/enemies_new.gd` → `update_elite_*`
- 배치표·인터페이스: `data/elites.json`
- 검사: `tests/elites_tests.gd` / 측정: `tests/elites_bot_measure.gd`
- 방패병 확정 변경은 `docs/SHIELDBEARER.md`

> 이름은 가칭이고 시간·체력·피해는 전부 **시험값**이다. 사용자가 확정한 값은 방패병 85%뿐이다.
> 체력 표(`data/pacing.json enemy_hp`)는 이번 작업에서 **바꾸지 않았다.**

---

## 0. 공통 규칙

| 규칙 | 구현 |
|---|---|
| 예고 → 방향(위치) 확정 → 실행 → 빈틈 | 모든 연계가 `*_aim` → `*_lock` → 실행 → `recover`/`stagger` |
| 확정 뒤에는 추적하지 않는다 | 확정 상태에서 `e.dir` / `e.leap_at` / `e.emerge_at` / `e.slam_at`을 고정하고 그 뒤 갱신하지 않는다. 검사 항목으로 고정 |
| 무조건 명중·회피 불가·강제 피해 할당 없음 | 모든 피해가 `st.damage_player()`를 지난다(회피 무적·피격 보호가 그대로 작동). 사슬의 끌기도 **피해가 실제로 들어갔을 때만** 발생한다 |
| 위험 공격 동시 제한 | 기존 `CombatState.may_attack` 경로를 그대로 쓴다. **보완**으로 "정예 동시 연계 1"을 더했다(`PEnemiesNew.elite_may_start`) |
| 예고 도형 공유 | `PEnemiesNew.elite_threats()`가 봇(`PBot.threats`)과 화면이 같은 기하를 읽게 한다 |
| 빈틈 보너스 | `recover`/`stagger` 중에는 기존 규칙대로 피해 ×1.5 |

### 정예 동시 연계 1 (보완한 부분)

일반 전투는 `overlap_limit`이 0이라 `may_attack`이 **항상 참**이다(동시 제한이 사실상 꺼져 있다 — 늑대 돌진만
`grant_dash_slots`로 따로 제한된다). 여기에 정예 연계까지 겹치면 탈출이 불가능해질 수 있으므로,
정예끼리만 "같은 순간에 연계 중인 정예는 하나"라는 조건을 추가했다.

- 일반 적의 압박은 **전혀 줄이지 않는다**(모두가 한 마리씩 차례를 기다리는 상태가 되지 않는다).
- `elite_busy()`는 정예 7종만 본다. 보스·일반 적은 세지 않는다.
- 편성 단계의 상한(`data/elites.json placement.max_elites_alive`)과는 별개의 두 번째 안전장치다.

---

## 1. A. 정예 궁수 (`elite_archer`) — 추격 사수

| 단계 | 상태 | 시간(초) | 내용 |
|---|---|---:|---|
| 조준 | `aim` | 0.58 | 플레이어 추적 |
| 방향 고정 | `shot_lock` | 0.12 | **여기서 방향 확정** → 발사(피해 12) |
| 재조준 ×2 | `aim` → `shot_lock` | 0.38 + 0.12 | 발사 간격 **0.50초** |
| 부채꼴 예고 | `fan_aim` | 0.60 | 추적 |
| 부채꼴 확정 | `fan_lock` | 0.15 | 중앙 화살 = 예고 방향, ±22도로 3발(각 10) |
| 빈틈 | `recover` | **1.40** | 옆으로 움직이며 재장전 |

- 첫 발까지 **0.70초**(0.58 + 0.12), 이후 0.50초 간격 3발, 부채꼴 예고 0.75초. 한 연계 = 화살 6발 / 발사 판정 4회.
- 거리 유지 190~340. 장애물에 **1초 이상** 시야가 막히면 `reposition`(1.2초 측면 이동)으로 사격 위치를 바꾼다.

## 2. B. 정예 검사 (`elite_blademaster`) — 철갑 추격자

| 단계 | 상태 | 시간(초) | 내용 |
|---|---|---:|---|
| 첫 예고 | `dash1_aim` | **0.65** | 통로 예고(실제 경로와 같은 계산) |
| 확정 | `dash1_lock` | 0.12 | 방향 고정 |
| 돌진 베기 | `dash1` | 거리 240 / 속도 640 | 피해 20. **돌진 중 추적 회전 없음** |
| 새 방향 예고 | `dash2_aim` | **0.50** | 다시 추적 |
| 두 번째 돌진 | `dash2_lock` → `dash2` | 0.12 + — | 피해 20 |
| 방패 자세 | `guard` | **1.50** | **정면 직접 피해 완전 차단**(아래) |
| 내려찍기 | `slam_aim` | 0.50 | 앞 34 지점, 반지름 96, 피해 26 |
| 빈틈 | `recover` | **1.00** | |

- **붙어 있을 때**(거리 90 미만): `backoff`로 1.2초 물러나 보고, 그래도 붙어 있으면 돌진을 건너뛰고 곧장
  `guard` → `slam` 근접 연계로 간다(제자리 걸음 방지). 봇 측정에서 이 경로가 실제로 자주 나온다.

### 방패 자세와 일반 방패병의 차이 (반드시 구분)

| | 일반 방패병 | 정예 검사 방패 자세 |
|---|---|---|
| 정면 직접 피해 | ×0.15 (**85% 감소**) | **×0.0 (완전 차단)** |
| 정면 각 | 120도 | 120도 |
| 지속 시간 | 상태에 따라(접근·준비 중 계속) | **1.5초 한정** |
| 측면·후면 | 정상 | 정상 |
| 바닥·지속·기술 피해 | 정상(우회) | 정상(우회) |
| 방향 전환 | 2.2 rad/s | 1.0 rad/s(더 느리다) |

두 경감은 같은 함수(`PEnemiesNew.shield_mult`)를 지나며 **곱해지지 않는다**(가장 강한 하나만 `min`으로).

## 3. C. 피의 송곳니 (`elite_fang`) — 정예 늑대

| 단계 | 상태 | 시간(초) | 내용 |
|---|---|---:|---|
| 측면 접근 | `approach` | — | 정면이 아니라 옆(offset 80)으로 돌아 붙는다 |
| 짧은 물기 | `bite_aim` | 0.35 | 사거리 52 · 90도 · 피해 16 |
| 짧게 이탈 | `backoff` | 0.50 | 도약 거리 확보 |
| 착지 예고 | `leap_aim` | 0.60 | 착지 원이 플레이어를 따라온다 |
| 착지 확정 | `leap_lock` | 0.15 | **여기서 착지 위치 고정** |
| 도약 | `leap` | 0.38 | 공중(넉백 무시), 착지 반지름 74 · 피해 22 |
| 빈틈 | `recover` / `stagger` | 1.10 / **1.60** | 맞으면 1.1초, **빗나가면 1.6초** |

- "도약 실패 시 공격 기회": 착지 원 밖으로 나가면 빈틈이 1.6초로 길어지고 화면에 "빗나감 — 큰 빈틈!"이 뜬다.

## 4. D. 역병 조율사 (`elite_plaguecaller`) — 정예 포자

| 단계 | 상태 | 시간(초) | 내용 |
|---|---|---:|---|
| 투척 예고 | `throw_aim` | 0.50 | |
| 부풀기 | `swell` | 0.90 | 포자 3개가 원 + 번호(1·2·3)로 보인다 |
| 순차 폭발 | (같은 상태) | **0.45 간격** | 반지름 70 · 피해 16, 폭발 자리에 구름 |
| 재정비 | `recover` | 1.50 | 다음 연계까지 `castInterval` 2.5초 |
| 근접 대응 | `burst_aim` → | 0.45 | 96 이내 · 70도 좁은 분출 · 피해 18 · 빈틈 1.0 |

- 포자는 플레이어 주위 120 거리에 55도 간격으로 흩어 놓되, **놓고 난 뒤에도 16방향 중 6방향 이상이
  열려 있어야** 놓는다(`_exits_open`). 막히면 그 포자는 생략한다 — 전장을 통째로 봉쇄하지 않는다.
- 잔류 구름: 개체당 최대 **3개**, 수명 **2.6초**. 여기에 전역 적 장판 상한(`pacing.json zone_cap` 10개)도 그대로 적용된다.
- 재사용 시계(`cast_t`)는 상태와 무관하게 흐른다. 붙어서 분출만 반복해 주 연계를 못 보는 일이 없도록 한 것이다.

## 5. E. 사슬 집행자 (`elite_chainbreaker`)

| 단계 | 상태 | 시간(초) | 내용 |
|---|---|---:|---|
| 사슬 예고 | `chain_aim` | 0.60 | 직선(길이 330 · 폭 26). **바위에 막히면 예고 길이도 짧아진다** |
| 확정 | `chain_lock` | 0.15 | 방향 고정 |
| 발사 | `chain_fly` | 속도 620 | 피해 10 |
| 끌기 | `pull` | 0.35 | 70까지 끌어당김. **벽·바위는 그대로 막는다** |
| 강타 예고 | `slam_aim` | **0.55** | 강타 위치는 **예고 시작 때 이미 고정** |
| 강타 확정 | `slam_lock` | 0.15 | 반지름 92 · 피해 26 |
| 빈틈 | `recover` | 1.20 | |
| 빗나감 | `retract` | **1.40** | 회수 빈틈 |
| 근접 대응 | `sweep_aim` | 0.45 | 72 이내 · 160도 횡베기 · 피해 18 · 빈틈 0.9 |

- **회피 기회 보장**: 끌기가 끝난 뒤 강타까지 **0.70초**(0.55 + 0.15) 동안 원의 위치가 **움직이지 않는다.**
  플레이어 이동 220 × 0.70 = 154 > 강타 반지름 92 + 플레이어 반지름이므로 회피를 쓰지 않고 걸어서도 벗어난다.
  검사에서 "걸어 나가면 강타를 맞지 않는다"를 수치로 확인한다.
- 여기만 "예고 중 추적"의 예외다. 끌기로 자리를 강제한 직후에 다시 추적하면 회피할 방법이 사라지기 때문이며,
  코드 주석에도 그 이유를 남겼다.
- 사슬이 회피·피격 보호로 막히면 끌기가 일어나지 않고 그대로 회수 빈틈으로 간다.

## 6. F. 군단 기수 (`elite_standard`)

| 단계 | 상태 | 시간(초) | 내용 |
|---|---|---:|---|
| 깃발 설치 예고 | `plant_aim` | 0.80 | |
| 설치 | — | — | **파괴 가능한 구조물** `elite_banner`(체력 90 · 수명 20초) |
| 지휘(깃발이 살아 있는 동안) | — | — | 범위 220 안 아군: 이동 ×1.25 · 직접 피해 ×0.85 |
| 돌격 명령 | — | 6초마다 2마리 | 2.2초 동안 이동 ×1.5, **총 6회 예산** |
| 재배치 | `plant_aim` | — | 깃발이 부서지면 다시 세운다. **깃발 예산 2개** |
| 근접 | `slash_aim` | 0.50 | 60 · 110도 · 피해 16 · 빈틈 1.2 |

- **적을 새로 소환하지 않는다.** 이미 싸우고 있는 적의 이동만 바꾼다.
  깃발(구조물)은 경험치·금화를 주지 않으므로(`xp_for`가 구조물에 0) 반복 파괴가 수급 경로가 되지 않는다.
- 지휘받은 적도 위험 공격 동시 제한(`may_attack`)을 그대로 따른다. 명령은 이동만 바꾼다.
- **깃발과 기수 중 무엇을 먼저 칠지 선택 가능**: 깃발을 부수면 집결·방어 지원·명령이 즉시 끊긴다(0.25초 안에 자동 해제).
  기수를 먼저 잡으면 명령은 멈추지만 남은 깃발의 방어 지원은 수명이 끝날 때까지 유지된다.
- 표시: 깃발 범위 고리와 지휘받는 적 표시를 0.6초마다 내보낸다(전용 그림은 `render.gd` 담당 — 아래 §9).

## 7. G. 균열 채굴자 (`elite_miner`)

| 단계 | 상태 | 시간(초) | 내용 |
|---|---|---:|---|
| 잠행 | `dive` | 0.40 | |
| 지하 이동 | `under` | 0.6 ~ **1.6(상한)** | 0.12초마다 흔적. 추적 허용 |
| 출현 예고 | `warn` | 0.70 | **여기 시작 때 출현 위치 확정** |
| 솟구치기 | `erupt` | — | 반지름 82 · 피해 22 |
| 돌무더기 | — | — | 최대 2개 생성, 전장 상한 **4개**, 수명 **8초**, 체력 40(부술 수 있다) |
| 지상 빈틈 | `stagger` | **1.60** | |
| 재사용 | — | 7.0 | 그 사이에는 지상에서 곡괭이(50 · 90도 · 피해 15 · 예고 0.45) |

- "지하에만 오래 숨어 시간을 끌지 않는다": `underMax` 1.6초 상한이 있고, 가까워지면 0.6초 뒤 곧바로 출현한다.
- 돌무더기는 놓고 난 뒤에도 플레이어 주위 16방향 중 **6방향 이상이 열려 있어야** 놓는다. 자리가 없으면 생략한다.
- **알려진 한계**: 출구 검사는 *플레이어 주위*만 본다. 목표 전투의 거점·호위 경로·전장 출구는 검사하지 않으므로
  `data/elites.json`에서 목표 전투(호위·거점)에는 배치하지 않도록 표시해 두었다.

---

## 8. 봇 측정 결과 (판정이 아니라 계측)

`python tools/run_suites.py --suites elites_bot_measure --allow-adhoc`
1막 시작 빌드(기준 DPS 22) · 정예 1마리 · 시드 1·2·3 중앙값 · 최대 90초.
**봇 승패는 통과 조건이 아니다.** 봇은 사람이 아니며 보정도 끝나지 않았다(`docs/BOT_FRAMEWORK.md`).

| 정예 | 봇 | 체력 | 처치 시간(초) | 살아서 실행한 공격 수 | 연계 시작/완주 | 플레이어가 받은 피해 |
|---|---|---:|---:|---:|---:|---:|
| 정예 궁수 | balanced | 200 | 18.5 | 20 | 5 / 5 | 34 |
| 정예 궁수 | aggressive | 200 | 11.0 | 12 | 3 / 3 | 12 |
| 정예 검사 | balanced | 280 | 18.7 | 6 | 1 / 4 | 26 |
| 정예 검사 | aggressive | 280 | 18.0 | 6 | 1 / 4 | 78 |
| 피의 송곳니 | balanced | 190 | 10.8 | 9 | 3 / 3 | 22 |
| 피의 송곳니 | aggressive | 190 | 10.7 | 9 | 3 / 3 | 0 |
| 역병 조율사 | balanced | 205 | 11.4 | 6 | 3 / 3 | 0 |
| 역병 조율사 | aggressive | 205 | 9.6 | 6 | 2 / 2 | 18 |
| 사슬 집행자 | balanced | 220 | 11.5 | 7 | 2 / 2 | 18 |
| 사슬 집행자 | aggressive | 220 | 11.0 | 7 | 1 / 1 | 36 |
| 군단 기수 | balanced | 185 | 10.6 | 6 | 1 / 6 | 0 |
| 군단 기수 | aggressive | 185 | 8.2 | 4 | 1 / 4 | 0 |
| 균열 채굴자 | balanced | 215 | 13.7 | 6 | 2 / 2 | 0 |
| 균열 채굴자 | aggressive | 215 | 14.7 | 6 | 2 / 2 | 0 |

"연계 시작"은 접근 상태에서 주 연계로 들어간 횟수, "완주"는 그 연계의 마지막 판정까지 간 횟수다
(정예 검사는 근접 연계 `guard` → `slam`도 완주로 세고, 군단 기수는 근접 베기까지 센다).

### 군단 기수 + 호위 늑대 3 (지휘 확인)

| 봇 | 기수 처치 시간(초) | 깃발 설치 | 돌격 명령 발동 | 명령 예산 소진 | 추가 소환 |
|---|---:|---:|---:|---:|---|
| balanced | 14.7 | 1회 | 2회 | 2/6 | **없음(설계상 소환하지 않는다)** |
| aggressive | 10.9 | 1회 | 2회 | 2/6 | **없음** |

### 대조군 (강화된 1막 일반 적, 같은 조건)

| 적 | 봇 | 체력 | 처치 시간(초) | 실행한 공격 수 | 받은 피해 |
|---|---|---:|---:|---:|---:|
| 늑대 | balanced | 48 | 2.1 | **1** | 0 |
| 방패병 | balanced | 88 | 6.1 | 3 | 0 |

### 읽는 법

- **"살아서 실행한 공격 수"가 핵심 지표다.** 강화된 일반 늑대는 죽기 전에 1회밖에 못 때리지만,
  정예는 4~20회를 실행한다. 체력이 아니라 "행동을 끝까지 보여 주는가"로 만든 차이다.
- **"고유 연계 2회"** 목표: 사슬 집행자/aggressive 1회를 빼면 모든 조합에서 2회 이상 나온다.
  사슬 집행자는 그 대신 7회의 공격(사슬 2 + 횡베기 5)을 실행하므로 대응 기회 자체는 충분하다.
  더 확실히 하려면 체력을 220 → 260으로 올리는 것이 후보다(미적용).
- "받은 피해 0"인 줄이 있는 것은 봇이 확정 예고를 잘 피하기 때문이며, **회피가 통한다는 증거**이지
  위협이 없다는 뜻이 아니다. 정예 검사/aggressive의 78은 방패 자세로 붙었을 때의 압박을 보여 준다.
- 처치 시간 8~19초는 사용자가 제시한 정예 범위(8~15초)의 위·아래를 조금 넘나든다. 1막 기준이며
  2·3막은 §10의 체력 표를 넣은 뒤 다시 재야 한다.

---

## 9. 담당 밖 파일에 필요한 변경 (고치지 않고 남긴다)

| 파일 | 무엇 | 왜 |
|---|---|---|
| `scripts/game/render.gd` | 정예 7종·구조물 2종의 그림이 없다. 지금은 `draw_enemy`의 기본 경로(원 + 이름)로 그려진다 | 새 적 종류 전용 그림이 필요하다. 규칙 쪽에서는 예고 도형(`elite_threats`)과 표시 효과(`fx`/`text`)를 이미 내보낸다 |
| `scripts/game/render.gd:1464` | `draw_shieldbearer`의 `open` 판정에서 `bash_aim`을 빼야 한다 | 규칙이 바뀌어 준비 중에도 방패가 닫혀 있는데 화면은 열린 것처럼 그린다(`docs/SHIELDBEARER.md` §5) |
| `scripts/rules/observe.gd` | `threats_of()`에 정예 7종 항목이 없다 | 실력 프로필 봇(`PSkillBot`)은 이 목록을 읽는다. 없으면 정예 예고를 못 본다. `PBot`(`PEnemies.threats`)은 이미 본다 |
| `scripts/rules/weapons.gd:334, 487` | `from` 누락 2곳 | 방패 출처 판정이 플레이어 위치로 대체된다(`docs/SHIELDBEARER.md` §3) |
| `tools/suites.json` | `elites_tests`, `elites_bot_measure` 등록 | 아래 §11 |
| `data/pacing.json` · `data/growth.json` · `data/themes.json` · `scripts/rules/run.gd` | 배치·체력·경험치 | 아래 §10 |

`scripts/rules/combat_state.gd`에 손대야 하는 일은 **없었다.** 기존 API(`may_attack` · `damage_player` ·
`damage_enemy` · `spawn_enemy` · `add_zone` · `move_swept` · `beam_length` · `nearest_valid_pos`)만 사용했다.

---

## 10. 배치 인터페이스 (`data/elites.json`)

지금 정예를 꽂는 유일한 자리는 `PRun.template_waves`(`scripts/rules/run.gd:184~185`)이며 종류가
`"wolf_alpha"`로 **고정**되어 있다. 아래 두 곳만 바꾸면 배치표를 그대로 쓸 수 있다(배치 담당자 몫).

```gdscript
# scripts/rules/run.gd — template_waves() 안
if int(tpl.get("elites", 0)) > 0:
    wave.append({ "type": String(tpl.get("elite_type", "wolf_alpha")), "n": int(tpl.elites), "ref": float(tpl.elites) })
```

```json
// data/themes.json — 편성 항목에 한 줄
{ "id": "t2a_shields", "elites": 1, "elite_type": "elite_blademaster", ... }
```

값이 없으면 기존 늑대 우두머리라 회귀가 없다.

### 체력: 막별 표를 `data/pacing.json`에 넣어야 한다

정예 체력은 등급이 아니라 **막**으로 오른다(`PPacing.elite_hp`). 지금은 `enemy_hp.elite_by_act`에
행이 없어 **1막 값이 모든 막에 그대로 쓰인다.** 아래는 사용자 기준("그 막 주력 적의 3~5배",
주력 = 늑대 48 / 262 / 612)으로 만든 시험값이다.

| 정예 | 1막 | 2막 | 3막 | 주력 대비 |
|---|---:|---:|---:|---:|
| `elite_archer` | 200 | 1090 | 2550 | ×4.17 |
| `elite_blademaster` | 280 | 1530 | 3570 | ×5.83 (중장갑 88 대비 ×3.18) |
| `elite_fang` | 190 | 1040 | 2420 | ×3.96 |
| `elite_plaguecaller` | 205 | 1120 | 2610 | ×4.27 |
| `elite_chainbreaker` | 220 | 1200 | 2800 | ×4.58 |
| `elite_standard` | 185 | 1010 | 2360 | ×3.85 |
| `elite_miner` | 215 | 1170 | 2740 | ×4.48 |

(기존 늑대 우두머리 120 / 970 / 2280과 같은 자리다.)

### 경험치

`data/growth.json`의 `growth.XP_VALUE`에 항목이 없으면 **기본값 5**가 쓰인다(늑대 우두머리는 30).
권장 시험값은 `data/elites.json interface.growth_json.suggest`에 적어 두었다(30~40).
테마 편성 경로는 `formation.xp_map` 예산을 쓰므로 영향이 적지만, 그 밖의 경로에서 정예가
일반 적보다 적은 경험치를 주는 것을 막으려면 넣어야 한다.

### 배치 원칙(시험값, `data/elites.json placement`)

- 동시 생존 정예 **1마리**, 전투당 1막 1 / 2막 1 / 3막 2.
- 1막은 위험 편성(risk)에만 넣는다(첫 만남이 사고가 되지 않게).
- `elite_standard`는 **호위가 4마리 이상 있는 편성에만** 넣는다(혼자 두면 지휘할 대상이 없다).
- `elite_miner`는 목표 전투(호위·거점)에 넣지 않는다(§7 한계).
- 겹치면 안 되는 조합: 궁수 + 조율사(원거리 + 바닥), 검사 + 사슬(돌진 + 끌기), 조율사 + 서리술사/포자(바닥 중복).

---

## 11. 검사 실행 (공통 실행기 명세는 내가 고치지 않았다)

지금은 임시 실행으로 돌린다:

```
python tools/run_suites.py --suites elites_tests --allow-adhoc
python tools/run_suites.py --suites elites_bot_measure --allow-adhoc
```

`tools/suites.json` 담당자에게 아래 두 항목 추가를 요청한다.

```json
"elites_tests":       { "env": {}, "timeout_sec": 600, "desc": "방패병 정면 방어 확정값·특수 정예 7종 규칙" },
"elites_bot_measure": { "env": {}, "timeout_sec": 900, "desc": "특수 정예 7종 봇 계측(승패는 판정 아님)" }
```

`elites_tests`는 `groups.rules`에도 넣으면 좋다. `elites_bot_measure`는 계측이라 `all`에 넣지 않아도 된다.
